# 项目进度

## 文档信息
- 最近更新：2026-03-26
- 当前阶段：后端正式化改造持续推进，已完成数据库化本地落地，进入工程能力补齐与正式数据库切换准备阶段
- 当前状态：后端代码基线已提交并推送到远程分支 `origin/test1.0.0`

## 今日完成
- 完成后端从路由直连存储到 `router + schema + service + repository` 的分层改造
- 完成认证体系升级：接入 access token、refresh token、会话表与注销链路
- 完成笔记历史版本、版本恢复与回收站接口落地
- 完成后端持久化从 JSON 方案切换到 Prisma + SQLite 本地数据库运行时方案
- 完成 Prisma schema、Prisma config、本地数据库初始化与种子数据落地
- 完成关键操作日志写入 `operation_logs`，并将 `requestId` 贯穿到关键后端动作
- 完成后端测试增强，覆盖认证刷新、回收站、历史版本、操作日志链路
- 完成后端静态校验：`npx tsc -p tsconfig.node.json --noEmit`
- 完成后端测试校验：`npm run test:server`
- 完成代码规范校验：`npm run lint`
- 完成当前代码基线提交并推送远程：`refactor backend with prisma persistence foundation`
- 完成数据源配置收口，新增集中化配置与 `.env.example`
- 完成前后端本地启动与连通性验证，确认 `http://127.0.0.1:5173` 可通过代理访问数据库版后端

## 当前已具备能力
- 用户可注册、登录、刷新会话、退出登录
- 用户可查看个人工作台与个人设置
- 支持文件夹树展示、展开折叠、新建、重命名、删除
- 支持笔记列表查看、筛选、搜索、新建、删除、切换
- 支持富文本编辑、自动保存、保存状态展示
- 支持笔记历史版本查询与指定版本恢复
- 支持回收站列表、恢复与彻底删除
- 支持关键后端操作日志写库，日志可关联 `requestId`
- 支持 Prisma + SQLite 本地数据库持久化
- 支持通过 Vite 代理访问数据库版后端接口并完成真实读写

## 当前未完成
- PostgreSQL / MySQL 正式数据库尚未接入
- Prisma 正式 migration 流程尚未跑通
- Prisma CLI `db push` 在当前环境下仍需继续排查
- 缺少一键启动脚本与更完整的本地运行说明
- 登录后功能页“重复刷新”问题仍需继续观察验证
- 尚未完成 Vercel Preview / Production 的正式部署链路

## 已知风险与说明
- 当前 Prisma 本地运行时链路已可用，但 Prisma CLI 工具链在当前沙箱环境下仍存在 `db push` 无明细报错问题
- 当前本地数据库采用 SQLite，是正式数据库切换前的过渡方案，不是最终线上形态
- 当前后端开发启动仍依赖 `tsx`，在部分权限环境下可能出现 `spawn EPERM`
- Vite 与后端数据目录同仓库时，仍需保持忽略 `server/data` 变化，避免触发整页热更新

## 建议下一步
1. 优先排查 Prisma CLI `db push` / migration 问题，打通正式 migration 流程
2. 将 Prisma datasource 从本地 SQLite 平滑切换到 PostgreSQL 或 MySQL 目标环境
3. 补齐环境配置说明、数据库切换说明与本地开发说明
4. 继续验证登录后页面稳定性与自动保存链路
5. 推进 Vercel Preview 环境适配与部署验证

## 联调信息
- 前端默认地址：`http://127.0.0.1:5173`
- 后端默认地址：`http://127.0.0.1:3001`
- 前端通过 Vite 代理访问 `/api`
- 演示账号：`demo@example.com`
- 演示密码：`noteflow123`

## 已验证命令
- `npx tsc -p tsconfig.node.json --noEmit`
- `npm run test:server`
- `npm run lint`

## 2026-03-26 补充进展
- 已定位前端页面中文乱码根因：前端组件本身无编码问题，实际是后端 SQLite 示例数据残留历史乱码内容。
- 已新增统一种子定义，并在后端启动时自动修复演示账号下的乱码目录名、笔记标题、摘要与正文文本。
- 已重新验证接口联通：`GET /api/v1/folders/tree`、`GET /api/v1/notes` 以及经由 Vite 代理的 `/api/v1/notes` 均返回正常中文。
- 已将后端 Prisma datasource 从 SQLite 调整为 Neon PostgreSQL 目标形态，运行时改用 `@prisma/adapter-neon`。
- 已补充 Neon 所需环境变量示例：池化 `DATABASE_URL` 与直连 `DIRECT_URL`。
- 已将数据库集成测试改为显式开关模式：仅在设置 `NOTEFLOW_RUN_DATABASE_TESTS=1` 时执行真实数据库联调测试。
- 已完成真实 Neon 数据库 `npm run db:push`，数据库与 Prisma schema 已同步。
- 已完成真实运行时验证：`/api/v1/health` 返回 `datasource.provider = postgresql`。
- 已完成前后端联通验证：Vite 代理到 Neon 后端的登录、`/api/v1/me`、`/api/v1/notes` 链路正常。
