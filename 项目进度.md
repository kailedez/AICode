# 项目进度

## 文档信息
- 最近更新：2026-03-25
- 当前阶段：前后端联调完成一轮，进入稳定性收尾与后端正式化改造阶段
- 当前状态：今日任务已结束，开发服务待关闭

## 今日完成
- 完成前端工作台重构，形成侧边栏、笔记列表、编辑器三栏结构
- 完成登录页、注册页与基础鉴权交互
- 完成前端 `apiClient` 与后端真实接口对接，替换本地 mock 为 HTTP 调用主链路
- 完成后端基础服务搭建，提供 `auth`、`me`、`settings`、`folders`、`notes` 等接口
- 完成 JSON 文件持久化方案，满足本地开发验证
- 完成自动保存、目录管理、笔记创建删除、搜索过滤等主要功能闭环
- 完成后端接口测试 `server/tests/api.test.ts`
- 完成前端静态校验：`npm run lint`、`npx tsc -b`
- 修复前端请求后端返回 `502` 的问题
- 修复登录后闪回登录页的问题，将“登录成功”和“工作台初始化”拆分处理
- 关闭 React `StrictMode`，降低开发态重复执行干扰
- 为搜索和目录切换加入相同值保护，减少无意义刷新
- 将核心组件改为按字段订阅 store，减少整页联动重渲染
- 重构编辑器同步逻辑，避免服务端回写再次触发自动保存
- 为保存链路增加“内容未变化则跳过保存”的防重保护
- 为 store 与编辑器增加开发态调试日志，便于后续继续排查
- 修复 Vite 监听后端数据文件导致整页重载的风险，忽略 `server/data` 与本地日志文件变化
- 修复文档编码问题，统一为 UTF-8 中文内容

## 当前已具备能力
- 用户可注册、登录、退出登录
- 用户可查看个人工作台
- 支持文件夹树展示、展开折叠、新建、重命名、删除
- 支持笔记列表查看、筛选、搜索、新建、删除、切换
- 支持富文本编辑、命令面板、自动保存、保存状态展示
- 支持主题切换
- 支持本地后端持久化保存数据

## 当前未完成
- 登录后功能页“重复刷新”问题仍需继续验证是否已彻底修复
- 后端当前仍使用 JSON 文件持久化，尚未切换到 MySQL / Prisma
- 鉴权仍为演示级 token 方案，未实现 refresh token、密码加密存储、完整会话管理
- 回收站、历史版本、恢复流程尚未正式落地
- 缺少统一的一键启动脚本
- 缺少更完整的本地运行说明与联调说明

## 已知风险与说明
- 当前后端开发启动依赖 `tsx`，在部分权限环境下可能出现 `spawn EPERM`
- Vite 与后端数据文件同仓库时，若未忽略 `server/data`，可能触发整页热更新
- 由于尚未切换数据库，当前数据结构已按正式后端设计，但实现仍属于开发态持久化方案

## 建议下一步
1. 优先继续验证并收尾“登录后功能页重复刷新”问题，必要时将后端数据目录迁出前端工程监听范围
2. 将后端持久化方案升级为 MySQL + Prisma
3. 完善鉴权体系，包括密码加密、refresh token、会话失效与注销机制
4. 补齐回收站、历史版本、恢复接口
5. 增加一键启动脚本与更清晰的开发说明

## 联调信息
- 前端默认地址：`http://127.0.0.1:5173`
- 后端默认地址：`http://127.0.0.1:3001`
- 前端通过 Vite 代理访问 `/api`
- 演示账号：`demo@example.com`
- 演示密码：`noteflow123`

测试
